# Runtime
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
pytz>=2024.1

# Testing (not installed in production Docker image)
pytest>=8.1.0
httpx>=0.27.0          # required by FastAPI TestClient
requests>=2.31.0       # used by standalone test_client.py
